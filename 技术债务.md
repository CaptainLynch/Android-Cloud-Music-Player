# 技术债务 (Technical Debt)

> 本文档追踪**已知但未立即处理**的架构问题与设计妥协。  
> AI 指令: 编码前读取本文档，避免在已有债务区域引入新复杂度。  
> 与「待开发池」区分：待开发池记录新功能需求，本文档记录现有代码的已知问题。

---

## AI 记忆锚点（每次会话必读）

- 无 DI 框架（AD-001）是**有意决策**，不是债务
- FavoritesRepository 单例模式（AD-007）是**有意决策**，不是债务
- 债务处理优先级：🔴 阻塞 > 🟠 高风险 > 🟡 中风险 > 🟢 低风险

---

## 活跃债务

| ID | 问题描述 | 引入迭代 | 风险等级 | 影响范围 | 解决成本 | 决策/备注 |
|----|---------|---------|---------|---------|---------|----------|
| TD-001 | FavoritesRepository `object` 单例导致单元测试困难（无法 Mock） | 迭代 11 | 🟡 中 | 测试覆盖 | 高（需改 5+ 处调用） | 见 AD-007。MVP 阶段接受，正式版前必须解决 |
| TD-002 | 无 DI 框架，ViewModel 手动创建，新增 ViewModel 时需修改 MainActivity | 迭代 1 | 🟡 中 | 可维护性 | 高（引入 Hilt 需改全项目） | 见 AD-001。迭代 15 后评估是否引入 Hilt |
| TD-003 | `NeteaseSettings` 同时管理认证设置和通用设置（主题/定时），职责混杂 | 迭代 12 | 🟡 中 | 可维护性 | 低 | 计划拆分为 `NeteaseSettings` + `AppSettings` |
| TD-004 | `SearchViewModel` 职责过重（搜索 + 收藏 + 定时关闭），违反单一职责 | 迭代 14 | 🟠 高 | 可维护性 | 中 | 计划拆分为 `SearchViewModel` + `TimerViewModel` |
| TD-005 | 播放队列无持久化，App 杀死后队列丢失 | 迭代 2 | 🟡 中 | 用户体验 | 中 | 依赖 TD-001 解决（需 Repository 支持） |
| TD-006 | `Theme.kt` 亮色/暗色色值未经过设计评审，可能存在对比度问题 | 迭代 14 | 🟢 低 | 视觉体验 | 低 | 暗色模式上线后收集反馈再调整 |

---

## 已解决债务

| ID | 问题描述 | 解决迭代 | 解决方式 |
|----|---------|---------|---------|
| TD-007（原） | SharedPreferences + Room 双轨持久化 | 迭代 11 | AD-006: 统一为 Room 单一数据源 |
| TD-008（原） | MusicService 与 MediaPlaybackService 重复 | 迭代 11 | 删除 MusicService，统一为 MediaPlaybackService |
| TD-009（原） | 直连/代理双模式增加维护成本 | 迭代 13 | AD-010: 移除直连模式，统一代理 |

---

## 债务处理流程

### 发现债务
任何角色在编码中如发现技术债务，按以下格式追加到「活跃债务」表：

```markdown
| TD-XXX | [一句话描述] | [当前迭代] | [🔴🟠🟡🟢] | [影响范围] | [低/中/高] | [备注] |
```

### 处理债务
1. **产品经理**定期审查（每 2 个迭代），决定哪些债务需要转化为任务
2. **架构师**在任务分配时评估债务影响，如解决某债务是某需求的前置条件，优先处理
3. **处理完成后**：将条目从「活跃债务」移至「已解决债务」，记录解决方式

### 当前迭代关联

> 迭代 14 涉及债务：TD-003（NeteaseSettings 拆分）可作为 F13/F14 的一部分自然处理

---

## 最后更新

2026-05-29 — 新增 TD-004 ~ TD-006，标记 TD-003 可在迭代 14 中处理
