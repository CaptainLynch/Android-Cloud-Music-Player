# 技术规范 (Technical Standards)

> 本文档为**权威技术规范**，所有角色编码时必须遵守。  
> AI 指令: 编码前先读取本文档，确保输出符合规范。如有冲突，以本文档为准。

---

## AI 记忆锚点（每次会话必读）

- 语言: **简体中文**（所有自然语言输出）
- 架构: **MVVM + StateFlow**，无 DI 框架，无 Jetpack Navigation
- UI: **纯 Kotlin + Compose**，禁止 Java / XML 布局
- 导航: 单 `MainActivity` + 手动状态管理
- 单例: Kotlin `object`（非 Hilt/Dagger/Koin）
- 最低 SDK: 24，目标 SDK: 36

---

## 1. 命名规范

### 1.1 文件与类

| 类型 | 命名规则 | 示例 |
|------|---------|------|
| Composable 函数 | PascalCase，描述 UI 功能 | `SearchScreen`, `SongItem`, `MiniPlayerBar` |
| ViewModel | 对应 Screen 名 + ViewModel | `SearchViewModel`, `FavoritesViewModel` |
| Repository | 功能名 + Repository | `FavoritesRepository` |
| 数据类 | 名词，反映数据结构 | `Song`, `FavoriteEntity`, `PlaylistResponse` |
| 单例对象 | 功能名 + Manager/Service | `MusicPlayerManager` |
| 接口 | 功能名 + 技术后缀 | `MusicApiService`, `FavoriteDao` |

### 1.2 变量与函数

| 类型 | 规则 | 示例 |
|------|------|------|
| Composable 参数 | camelCase，描述用途 | `onSongClick`, `isFavorite`, `modifier` |
| StateFlow 属性 | `_xxx` (Mutable) / `xxx` (公开) | `_searchResults` / `searchResults` |
| 普通变量 | camelCase | `currentSong`, `playlistId` |
| 常量 | UPPER_SNAKE_CASE | `BASE_URL`, `DEFAULT_TIMEOUT` |
| 挂起函数 | 动词开头 | `loadPlaylist()`, `toggleFavorite()` |
| 普通函数 | 动词开头，返回值描述 | `formatDuration()`, `isNetworkAvailable()` |

---

## 2. Compose 编码铁律

### 2.1 必遵守

- [ ] 所有字符串使用 `stringResource(R.string.xxx)`，禁止硬编码
- [ ] 所有颜色使用 `MaterialTheme.colorScheme.xxx`，禁止硬编码色值（图标/插图除外）
- [ ] 每个 `Screen` 级 Composable 必须提供 `@Preview`
- [ ] UI 层**禁止**直接调用 Repository / Dao，必须通过 ViewModel
- [ ] Composable 参数接收 `Modifier` 并置于首位（如有）
- [ ] 状态使用 `collectAsState()` 订阅 StateFlow，禁止直接暴露 MutableStateFlow 给 UI

### 2.2 禁止事项

- [ ] 禁止在 Composable 中执行网络请求 / 数据库操作
- [ ] 禁止在 Composable 中使用 `rememberCoroutineScope()` 发起非 UI 操作
- [ ] 禁止 Composable 持有 Context / Application 引用
- [ ] 禁止在 `LazyColumn` item 中使用非稳定键（必须提供 `key` 参数）
- [ ] 禁止嵌套过深的 Composable（超过 5 层需拆分）

### 2.3 状态管理

```kotlin
// ✅ 正确 — ViewModel 管理状态
class SearchViewModel : ViewModel() {
    private val _searchResults = MutableStateFlow<List<Song>>(emptyList())
    val searchResults: StateFlow<List<Song>> = _searchResults.asStateFlow()
}

// UI 层
val results by viewModel.searchResults.collectAsState()

// ❌ 错误 — UI 层直接管理业务状态
@Composable
fun BadScreen() {
    var results by remember { mutableStateOf(emptyList<Song>()) }
    // 不要这样
}
```

---

## 3. 架构分层红线

### 3.1 分层职责

```
UI Layer (Compose Screen)
    ↓ 调用（仅观察状态）
ViewModel Layer (StateFlow + Coroutines)
    ↓ 调用
Data Layer (Repository → Retrofit / Room / DataStore)
    ↓ 调用
System Layer (ExoPlayer / MediaSession / Android System)
```

### 3.2 禁止跨层调用

| 错误示例 | 正确做法 |
|---------|---------|
| Composable 直接调用 `RetrofitClient.api.searchMusic()` | 通过 ViewModel → Repository → ApiService |
| ViewModel 直接操作 `ExoPlayer` 实例 | 通过 `MusicPlayerManager` 单例接口 |
| Repository 持有 `Context` 做 Toast | Repository 返回 Result，ViewModel 处理错误状态 |
| `Activity` 中直接修改 `SearchViewModel` 的 `_searchResults` | 通过 ViewModel 的公开方法 |

### 3.3 ViewModel 规范

```kotlin
// ✅ 正确
class SearchViewModel : ViewModel() {
    private val _uiState = MutableStateFlow(SearchUiState())
    val uiState: StateFlow<SearchUiState> = _uiState.asStateFlow()
    
    fun search(query: String) {
        viewModelScope.launch {
            _uiState.update { it.copy(isLoading = true) }
            try {
                val results = repository.search(query)
                _uiState.update { it.copy(results = results, isLoading = false) }
            } catch (e: Exception) {
                _uiState.update { it.copy(error = e.message, isLoading = false) }
            }
        }
    }
}

// ✅ 正确 — UI State 封装
data class SearchUiState(
    val query: String = "",
    val results: List<Song> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null
)
```

---

## 4. 网络层规范

### 4.1 Retrofit 接口

```kotlin
// ✅ 正确
interface MusicApiService {
    @GET("api.php")
    suspend fun searchMusic(
        @Query("source") source: String,
        @Query("name") name: String
    ): List<Song>
    
    @GET("api.php")
    suspend fun getSongUrl(@Query("id") id: String): SongUrlResponse
}
```

### 4.2 错误处理

```kotlin
// ✅ 正确 — Repository 层封装 Result
suspend fun search(query: String): Result<List<Song>> = runCatching {
    api.searchMusic(query)
}.onFailure { e ->
    // 记录日志，不直接 UI 操作
    Log.e(TAG, "Search failed", e)
}

// ViewModel 中处理
viewModelScope.launch {
    repository.search(query)
        .onSuccess { /* 更新状态 */ }
        .onFailure { /* 更新错误状态 */ }
}
```

### 4.3 协程作用域

| 场景 | 作用域 | 说明 |
|------|--------|------|
| 网络请求 | `viewModelScope.launch` | 与 ViewModel 生命周期绑定 |
| 数据库操作 | `viewModelScope.launch` | Room 支持挂起函数 |
| 倒计时/定时器 | `viewModelScope.launch` + `timer {}` 或 `delay` | 必须保存 Job 以便取消 |
| 后台播放 | `ServiceLifecycleDispatcher` / `coroutineScope` | 在 Service 中使用 |

---

## 5. 持久化规范

### 5.1 Room

- Entity 必须标注 `@Entity(primaryKeys = [...])`
- Dao 接口方法使用 `@Insert(onConflict = OnConflictStrategy.REPLACE)` 明确冲突策略
- 数据库版本变更必须提供 `Migration`（不允许 destructive migration 在生产环境）
- TypeConverter 放在独立文件，命名 `XxxTypeConverters`（复数，避免与 `@TypeConverters` 注解混淆）

### 5.2 DataStore

- 键名定义为顶层常量：`val KEY_THEME_MODE = stringPreferencesKey("theme_mode")`
- 读写封装在 Repository/Settings 类中，禁止 ViewModel 直接操作 DataStore
- 使用 `map {}` 暴露 Flow，UI 层通过 `collectAsState()` 订阅

---

## 6. ID 命名规范（全局统一）

所有项目 ID 遵循统一格式，确保跨文件引用时不冲突。

| 类型 | 格式 | 示例 | 分配规则 |
|------|------|------|---------|
| 需求 | `F{序号}` | F14, F15 | 产品经理递增分配 |
| Bug | `BUG-{序号}` | BUG-005 | 产品经理递增分配 |
| 架构决策 | `AD-{序号}` | AD-011 | 架构师递增分配 |
| 任务 | `TASK-{角色}-{序号}` | TASK-FE-501, TASK-BE-501 | 架构师分配，角色前缀 FE/BE/TE |
| 代码变更 | `CHG-{角色}-{序号}` | CHG-FE-102, CHG-BE-201 | 各角色自行递增 |
| 测试 | `TEST-{角色}-{序号}` | TEST-FE-101, TEST-BE-201 | 测试工程师分配 |
| 测试报告 | `TR-{年}-{序号}` | TR-2026-012 | 测试工程师递增 |
| 技术债务 | `TD-{序号}` | TD-001 | 任何角色发现后记录 |
| 迭代回顾 | `RETRO-{序号}` | RETRO-14 | 自动对应迭代号 |

---

## 7. Git 提交规范

```
{type}({scope}): {subject}

{body}

Refs: {TASK/CHG ID}
```

| Type | 用途 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `refactor` | 重构（无功能变更）|
| `docs` | 文档更新（MD 文件）|
| `test` | 测试相关 |
| `chore` | 构建/工具链 |

**示例**:
```
feat(search): 添加定时关闭弹窗 UI

- SearchScreen 顶栏添加定时器图标按钮
- 点击弹出 AlertDialog 选择时间（15/30/60/自定义）
- SearchViewModel 新增倒计时协程

Refs: TASK-FE-501, CHG-FE-601
```

---

## 8. 代码审查检查清单

### 8.1 前端审查

- [ ] 符合 Compose 编码铁律（第 2 节）
- [ ] 无跨层调用（第 3.2 节）
- [ ] StateFlow 正确使用（第 3.3 节）
- [ ] Composable 提供 `@Preview`
- [ ] 无硬编码字符串/颜色
- [ ] 内存安全（Effect / DisposableEffect 正确使用）

### 8.2 后端审查

- [ ] 网络请求在 ViewModel 协程中处理
- [ ] 正确使用 Retrofit 封装接口
- [ ] 无主线程阻塞操作
- [ ] StateFlow 正确暴露数据状态
- [ ] 无内存泄漏（ViewModel 中正确释放协程）
- [ ] MusicPlayerManager 资源正确释放

---

## 最后更新

2026-05-29 — 新增：统一 ID 命名规范、Git 提交规范、AI 记忆锚点
