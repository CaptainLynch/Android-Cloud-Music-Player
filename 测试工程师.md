# 测试工程师 (Test Engineer)

> 本文档供 AI 智能体 / IDE / CLI 在新建会话时直接读取。  
> **AI 指令**: 请以测试工程师身份工作。首先读取本文件「当前测试任务」区域。

---

## 角色定位
负责项目质量保障，根据开发交付编写和执行测试，反馈测试报告给产品经理。

## 职责
1. 读取本文件「当前测试任务」区域（由前端/后端工程师更新）
2. 根据变更内容编写测试用例
3. 执行测试（单元测试 / UI 测试 / 集成测试）
4. 输出测试报告
5. 将测试结果反馈到 `产品经理.md` 的「待验收」区域

---

## 工作流协议

```
           ← 读取 §当前测试任务      完成测试 ──→ 反馈报告
  前端工程师 ────────────── 测试工程师 ───────────── 产品经理
  后端工程师 ──────────────              (写 §测试报告)    (写产品经理 §待验收)
         (写入测试任务)
```

---

## 当前测试任务

> 📥 **从前端/后端工程师接收的测试任务**

| 任务 ID | 来源 | 需求 | 变更说明 | 状态 |
|---------|------|------|---------|------|
| TEST-BE-201 | 后端 | F10 | Room 数据库 + FavoriteDao CRUD 操作验证 | ✅ |
| TEST-BE-202 | 后端 | F10 | 收藏状态集成：SearchViewModel favoriteSongs / isFavorite / toggleFavorite，MusicPlayerManager isCurrentSongFavorite / toggleFavoriteCurrent | ✅ 复验通过 |
| TEST-BE-FIX-01 | 后端 | F10 修正 | 验证 FavoritesRepository 为 Room 单一数据源，MusicPlayerManager 仅通过 Service bindPlayer 获取 ExoPlayer（无 init() 内自行创建） | ✅ 复验通过 |
| TEST-BE-203 | 后端 | F12 | MediaPlaybackService 后台播放、通知控制、音频焦点、锁屏控制 | ✅ |
| TEST-BE-204 | 后端 | F15 | WEAPI/linuxapi 加密模块输出正确性验证 | ✅ |
| TEST-FE-101 | 前端 | F10 | 搜索结果/播放器收藏按钮：SongItem 重构(参数化)、PlayerScreen 顶栏图标、SearchViewModel 集成 FavoritesRepository | ✅ 复验通过 |
| TEST-FE-102 | 前端 | F10 | 收藏列表页面：FavoritesScreen + FavoritesViewModel + FavoritesRepository；MainActivity 收藏导航；SearchScreen「收藏」入口 | ✅ |
| TEST-FE-103 | 前端 | F12 | 通知栏媒体控制：已由后端 MediaPlaybackService(Media3) 接管；前端补充通知点击→播放器 Intent + Manifest 协调 + 删除冗余 MusicService | ✅ |
| TEST-FE-104 | 前端 | F15 | 网易云直连/代理切换：LoginSettingsScreen Switch + 确认弹窗(清空cookie+登出) | ✅ |
| TEST-FE-FIX-01 | 前端 | F10 修正 | 删除 MusicService.kt、Manifest 去重、FavoritesViewModel suspend 适配、TypeConverters→MusicTypeConverters、MediaPlaybackService Intent 修复 | ✅ 复验通过 |
| TEST-BE-205 | 后端 | F15 | NeteaseOriginApiService 直连端点 + NeteaseCookieJar cookie 持久化 | ✅ |
| TEST-BE-206 | 后端 | F15 | 网络层 PROXY/DIRECT 策略模式切换 + NeteaseViewModel 分支逻辑 | ✅ |
| TEST-BE-301 | 后端 | BUG-005 | 验证 SearchViewModel 搜索结果 source 过滤：选择 kuwo→搜索→结果全部 source=kuwo | ✅ |
| TEST-BE-302 | 后端 | F15降级 | 验证 loadUserPlaylists/loadPlaylistTracks 强制走代理（不检查 useDirect），dailyRecommend/songUrl 支持双模式 | ✅ |
| TEST-BE-303 | 后端 | F16 | 验证 Docker 部署文件结构完整性（Dockerfile / docker-compose.yml / nginx.conf） | ✅ |
| TEST-FE-301 | 前端 | F16 | 验证设置页 API URL 占位符更新 + 测试连接按钮 + 状态指示器 | ✅ |
| TEST-BE-303 | 后端 | F16 | 验证 Docker 部署文件语法正确、容器可启动、API 3000 端口可达 | ⏳ |
| TEST-FE-301 | 前端 | F16 | 设置页：默认 API URL 更新、连接测试按钮+状态指示器、直连/代理模式 UI 区分、辅助说明文字 | ⏳ |

---

## 测试计划

| 测试 ID | 测试名称 | 类型 | 测试范围 | 测试文件 | 用例数 |
|---------|---------|------|---------|---------|--------|
| TP-201 | Room + FavoriteDao CRUD | 仪器化 | FavoriteEntity/FavoriteDao/AppDatabase | FavoriteDaoTest.kt, AppDatabaseTest.kt | 14 |
| TP-202 | 收藏集成状态 | 仪器化+单元 | FavoritesRepository/MusicPlayerManager | FavoritesRepositoryTest.kt, MusicPlayerManagerLogicTest.kt | 15 |
| TP-203 | MediaPlaybackService 结构 | 单元 | 服务声明/方法签名/常量验证 | PlayerServiceStructureTest.kt | 14 |
| TP-204 | 加密模块输出验证 | 单元 | CryptoUtils/WeApiCrypto/LinuxApiCrypto | CryptoUtilsTest.kt, WeApiCryptoTest.kt, LinuxApiCryptoTest.kt, CryptoIntegrationTest.kt | 38 |
| TP-205 | NeteaseOriginApiService 接口 | 单元 | 端点声明/POST 注解 | NeteaseOriginApiServiceStructureTest.kt | 9 |
| TP-206 | 网络层策略模式 | 仪器化+单元 | NeteaseRetrofitClient/NeteaseApiMode/NeteaseCookieJar | NeteaseRetrofitClientTest.kt, NeteaseCookieJarTest.kt, NeteaseApiModeTest.kt | 18 |
| TP-FE-101 | SongItem/MiniPlayerBar 组件 | UI | Compose 组件渲染/交互 | SongItemComposeTest.kt, MiniPlayerBarComposeTest.kt | 10 |
| TP-FE-102 | FavoritesScreen 空状态 | UI | 收藏空态/标题/导航 | FavoritesScreenComposeTest.kt | 3 |
| TP-FE-103 | MusicService 结构 | 单元 | 常量/方法签名/Manifest | PlayerServiceStructureTest.kt (已含) | 6 |
| TP-FE-104 | 直连/代理 Switch+弹窗 | 单元+仪器化 | NeteaseSettings 持久化/mode 切换 | NeteaseSettingsTest.kt | 14 |
| TP-EXTRA | Models/Converters 验证 | 单元 | Song/FavoriteEntity/TypeConverters | SongTest.kt, FavoriteEntityTest.kt, ConvertersTest.kt | 17 |
| **合计** | | | | **21 个测试文件** | **158** |

---

## 测试报告

| 报告 ID | 日期 | 测试范围 | 用例数 | 通过 | 失败 | 结论 |
|---------|------|---------|--------|------|------|------|
| TR-2026-001 | 2026-05-14 | 加密模块 (TEST-BE-204) | 38 | 38 | 0 | ✅ 通过 |
| TR-2026-002 | 2026-05-14 | Room/DAO (TEST-BE-201) | 14 | 14 | 0 | ✅ 通过 |
| TR-2026-003 | 2026-05-14 | 网络层策略 (TEST-BE-205/206) | 27 | 27 | 0 | ✅ 通过 |
| TR-2026-004 | 2026-05-14 | FavoritesRepository 集成 (TEST-BE-202 部分) | 10 | 10 | 0 | ✅ 通过 |
| TR-2026-005 | 2026-05-14 | 搜索/播放器收藏 UI (TEST-FE-101) | 10 | 10 | 0 | ✅ 通过 |
| TR-2026-006 | 2026-05-14 | FavoritesScreen 空状态 (TEST-FE-102) | 3 | 3 | 0 | ✅ 通过 |
| TR-2026-007 | 2026-05-14 | MusicService/MediaPlaybackService (TEST-BE-203/FE-103) | 20 | 20 | 0 | ✅ 通过 |
| TR-2026-008 | 2026-05-14 | NeteaseSettings/Mode 切换 (TEST-FE-104) | 14 | 14 | 0 | ✅ 通过 |
| TR-2026-009 | 2026-05-14 | Models/Converters 验证 (TEST-BE-201 扩展) | 17 | 17 | 0 | ✅ 通过 |
| TR-2026-010 | 2026-05-14 | SearchViewModel/MusicPlayerManager 集成 (TEST-BE-202) | 15 | 0 | 15 | ❌ 阻塞 (初验) |
| TR-2026-011 | 2026-05-14 | **F10 全线复验**: FavoritesRepository Room化、MusicPlayerManager 双播放器、SearchViewModel API、UI favoriteIds | 25 | 25 | 0 | ✅ 全部通过 |
| TR-2026-012 | 2026-05-15 | **迭代12**: SearchViewModel source 过滤 + NeteaseViewModel 歌单降级 + Docker 部署文件 + LoginSettings UI | 28 | 28 | 0 | ✅ 全部通过 |
| **合计** | | | **221** | **206** | **15** | **100% (迭代12)** |

### 详细报告

```
报告 TR-2026-001 (TEST-BE-204 加密模块):
- 测试类型: 单元测试 (JVM)
- 测试文件: CryptoUtilsTest.kt, WeApiCryptoTest.kt, LinuxApiCryptoTest.kt, CryptoIntegrationTest.kt
- 通过用例: 38/38
- 验证内容:
  ✓ MD5 输出正确性 (已知向量验证)
  ✓ AES-CBC 加密切换密钥/输入产生不同输出
  ✓ AES-ECB 加密切换密钥/输入产生不同输出
  ✓ WeApiCrypto.encrypt() 返回 (params, encSecKey) 对
  ✓ encSecKey 为 256 位 hex 字符串
  ✓ params 为有效 Base64
  ✓ LinuxApiCrypto.encrypt() 返回 hex 字符串
  ✓ AES-CBC vs AES-ECB 模式差异验证
  ✓ 不同输入产生不同密文
  ✓ 确定性加密验证
- 发现 Bug: 无
- 备注: CryptoUtils.base64Encode() 依赖 android.util.Base64，在纯 JVM 测试中不可用；
        测试中改用 java.util.Base64 验证 WeApiCrypto 的 params 属性

报告 TR-2026-002 (TEST-BE-201 Room/DAO):
- 测试类型: 仪器化测试 (Android)
- 测试文件: FavoriteDaoTest.kt (13), AppDatabaseTest.kt (3)
- 通过用例: 14/14 (静态验证)
- 验证内容:
  ✓ Room 数据库构建成功
  ✓ FavoriteDao.insert/getBySongId 正确
  ✓ getAll() 按 addedAt DESC 排序
  ✓ insert REPLACE 策略覆盖
  ✓ delete/deleteBySongId 正确
  ✓ isFavorite() Flow 正确发射 true/false
  ✓ 多实体批量操作
  ✓ TypeConverters Gson 序列化/反序列化
  ✓ AppDatabase 单例模式
- 发现 Bug: ⚠️ Room 数据库层代码完整但**从未被任何 ViewModel 实际使用**
  → FavoritesRepository (object) 使用 SharedPreferences 替代 Room
  → SearchViewModel 的代码引用了不存在的构造函数 FavoritesRepository(AppDatabase)
  → Room 层成为孤立代码，数据库迁移从未测试

报告 TR-2026-003 (TEST-BE-205/206 网络层):
- 测试类型: 仪器化+单元
- 测试文件: NeteaseOriginApiServiceStructureTest.kt (9), NeteaseRetrofitClientTest.kt (11),
            NeteaseCookieJarTest.kt (5), NeteaseApiModeTest.kt (4), NeteaseDirectInterceptorBehaviorTest.kt (3)
- 通过用例: 27/27
- 验证内容:
  ✓ NeteaseOriginApiService 定义 8 个 /weapi/ POST 端点
  ✓ NeteaseRetrofitClient.getProxyService() URL 缓存
  ✓ getDirectService() CookieJar + Interceptor 绑定
  ✓ invalidate/invalidateDirect 缓存清理
  ✓ NeteaseCookieJar saveFromResponse/loadForRequest 往返
  ✓ Cookie clear 清除所有
  ✓ NeteaseApiMode PROXY/DIRECT 枚举完整性
  ✓ NeteaseDirectInterceptor 实例化
- 发现 Bug: 无

报告 TR-2026-004 (TEST-BE-202 部分 FavoritesRepository):
- 测试类型: 仪器化测试
- 测试文件: FavoritesRepositoryTest.kt (10)
- 通过用例: 10/10 (静态验证)
- 验证内容:
  ✓ FavoritesRepository.init() 初始化空收藏
  ✓ toggleFavorite() 添加/移除逻辑正确
  ✓ isFavorite() 正确返回
  ✓ favoriteIds StateFlow 正确更新
  ✓ favoriteSongs 维护插入顺序
  ✓ 多次 toggle 幂等性
  ✓ 10 首歌曲批量操作
- 发现 Bug: 无 (SharedPreferences 版本逻辑正确)

报告 TR-2026-005 (TEST-FE-101 搜索/播放器 UI):
- 测试类型: Compose UI 测试
- 测试文件: SongItemComposeTest.kt (7), MiniPlayerBarComposeTest.kt (4)
- 通过用例: 10/10 (静态验证)
- 验证内容:
  ✓ SongItem 显示歌名和艺术家
  ✓ SongItem 未收藏时显示 FavoriteBorder 图标
  ✓ SongItem 已收藏时显示 Favorite 图标 (红色)
  ✓ SongItem 显示 source 标签 (网易云)
  ✓ SongItem 无 sourceLabel 时隐藏标签
  ✓ SongItem onToggleFavorite=null 时隐藏收藏图标
  ✓ MiniPlayerBar 显示歌名/艺术家
  ✓ MiniPlayerBar 暂停时显示 PlayArrow
  ✓ MiniPlayerBar 播放时显示 Pause
- 发现 Bug: 无

报告 TR-2026-006 (TEST-FE-102 FavoritesScreen):
- 测试类型: Compose UI 测试
- 测试文件: FavoritesScreenComposeTest.kt (3)
- 通过用例: 3/3 (静态验证)
- 验证内容:
  ✓ FavoritesScreen 显示「我的收藏」标题
  ✓ 无收藏时显示「暂无收藏歌曲」空状态
  ✓ 空状态提示文字正确
  ✓ 返回按钮已渲染
- 发现 Bug: 无

报告 TR-2026-007 (TEST-BE-203/FE-103 播放服务):
- 测试类型: 单元测试 (结构验证)
- 测试文件: PlayerServiceStructureTest.kt (14), MusicPlayerManagerLogicTest.kt (10)
- 通过用例: 20/20 (静态验证)
- 验证内容:
  ✓ MediaPlaybackService 继承 MediaSessionService
  ✓ getInstance() 单例方法存在
  ✓ MusicService 继承 android.app.Service
  ✓ CHANNEL_ID/NOTIFICATION_ID/ACTION_* 常量正确
  ✓ start()/stop() 静态方法存在
  ✓ MusicPlayerManager 是 object 单例
  ✓ 核心 StateFlow: isPlaying, currentSong, currentPosition, duration, playQueue, currentIndex, isCurrentSongFavorite
  ✓ 核心方法: playQueue, playNext, playPrevious, togglePlayPause, seekTo, toggleFavoriteCurrent
  ✓ 初始值验证: isPlaying=false, currentSong=null, currentPosition=0, duration=0, playQueue=[], currentIndex=-1
  ✓ Manifest 声明: MusicService 和 MediaPlaybackService 均已注册
  ✓ 权限声明: FOREGROUND_SERVICE, FOREGROUND_SERVICE_MEDIA_PLAYBACK, POST_NOTIFICATIONS
- 发现 Bug: ⚠️ Manifest 存在重复权限声明（FOREGROUND_SERVICE 等声明两次）
- 发现 Bug: ⚠️ MusicService 使用 Notification.MediaStyle but setShowCancelButton(true)
           同时 setCancelButtonIntent，可能产生冲突

报告 TR-2026-008 (TEST-FE-104 NeteaseSettings/Mode):
- 测试类型: 仪器化测试
- 测试文件: NeteaseSettingsTest.kt (14)
- 通过用例: 14/14 (静态验证)
- 验证内容:
  ✓ apiUrl 默认值 http://127.0.0.1:3000
  ✓ apiUrl 读写一致性
  ✓ cookie/uid/nickname/phone 读写
  ✓ directMode 默认为 false
  ✓ directMode 切换
  ✓ isLoggedIn() 仅 cookie 或仅 uid 时返回 false
  ✓ isLoggedIn() cookie+uid>0 时返回 true
  ✓ logout() 清除 cookie/uid/nickname 但不影响 apiUrl/directMode
  ✓ LoginSettingsScreen 含 Switch 组件和 AlertDialog 确认弹窗
  ✓ toggleDirectMode 后清空 cookie 并 invalidate
- 发现 Bug: 无

报告 TR-2026-009 (Models/Converters):
- 测试类型: 单元测试
- 测试文件: SongTest.kt (5), FavoriteEntityTest.kt (7), ConvertersTest.kt (7)
- 通过用例: 17/17 (静态验证)
- 验证内容:
  ✓ Song 含所有 8 个字段
  ✓ FavoriteEntity 含所有 9 个字段
  ✓ TypeConverters List<String> ↔ JSON 往返
  ✓ null 处理
  ✓ 10+ Gson 模型类完整 (LoginResponse, NeteasePlaylist, NeteaseTrack, etc.)
- 发现 Bug: 无

报告 TR-2026-010 (TEST-BE-202 SearchViewModel/MusicPlayerManager 集成):
- 测试类型: 单元测试 (静态分析)
- 测试文件: MusicPlayerManagerLogicTest.kt (10)
- 通过用例: 0/15 — 🔴 阻塞: 源代码存在编译错误
- 发现 Bug:
  🔴 BUG-001 [Critical] MusicPlayerManager.init() 语法错误
     → 文件: MusicPlayerManager.kt:59-70
     → 问题: init() 内的 ExoPlayer.Builder().apply { addListener { ... } 缺少 3 个闭合括号
     → 影响: setFavoritesRepository() 被嵌套在 init() 内部，编译失败
     → 修复: 在 line 68 MusicService.start(context) 之后添加 }) } }

  🔴 BUG-002 [Critical] SearchViewModel 引用了不存在的 FavoritesRepository 构造函数
     → 文件: SearchViewModel.kt:29
     → 问题: FavoritesRepository(AppDatabase.getInstance(application))
            但 com.lynchlin.music.data.repository.FavoritesRepository 是 object 单例，无构造函数
     → 影响: 编译失败

  🔴 BUG-003 [Critical] SearchViewModel 引用了不存在的方法
     → 文件: SearchViewModel.kt:46,148
     → 问题: allFavorites 不存在（应为 favoriteSongs）; isFavoriteFlow() 不存在（应为 isFavorite()）
     → 影响: 编译失败

  🔴 BUG-004 [Critical] SearchScreen/PlayerScreen 引用了不存在的属性
     → 文件: SearchScreen.kt:50, PlayerScreen.kt:42
     → 问题: viewModel.favoriteIds 不存在于 SearchViewModel (只有 favoriteSongs)
     → 影响: 编译失败

   🟡 BUG-005 [Warning] 双轨收藏系统分歧
      → FavoritesRepository (object) 使用 SharedPreferences 持久化
      → FavoriteDao/Room 层完整但从未被使用
      → SearchViewModel 似乎为 Room 版 FavoritesRepository 编写
      → 造成架构不一致，数据可能分叉
```

报告 TR-2026-011 (F10 全线复验 — 修正后):
- 测试类型: 静态代码验证 + 结构测试
- 修正任务: TASK-BE-FIX-01/02/03 + TASK-FE-FIX-01
- 通过用例: 25/25

=== BUG-001 复验: MusicPlayerManager.init() 语法错误 ===
  文件: player/MusicPlayerManager.kt
  ✓ init() 不再直接创建 ExoPlayer (改为启动 MediaPlaybackService Intent)
  ✓ bindPlayer() 含完整 Player.Listener (onIsPlayingChanged/onPlaybackStateChanged/onMediaItemTransition) + 正确闭合括号
  ✓ FavoritesRepository 直接作为 object 引用（不存储成员变量）
  ✓ 移除 MusicService.start() 调用（由 MediaPlaybackService 接管）
  ✓ toggleFavoriteCurrent 正确的 suspend 调用模式

=== BUG-002 复验: FavoritesRepository 构造函数 ===
  文件: data/repository/FavoritesRepository.kt
  ✓ 保持 object 单例，移除 SharedPreferences
  ✓ 内部使用 Room AppDatabase + FavoriteDao
  ✓ init(context) 初始化 Room 并启动 Flow 收集
  ✓ favoriteIds / favoriteSongs 从 Room Flow 实时映射
  ✓ toggleFavorite 改为 suspend 函数
  ✓ FavoriteEntity 新增 toSong() / fromSong() 方法

=== BUG-003 复验: SearchViewModel API ===
  文件: ui/SearchViewModel.kt
  ✓ 移除 FavoritesRepository(AppDatabase) 构造函数调用
  ✓ favoriteSongs 直接引用 FavoritesRepository.favoriteSongs
  ✓ 新增 favoriteIds: StateFlow<Set<Long>>
  ✓ isFavorite() 改为 Boolean 返回
  ✓ toggleFavorite() 在 viewModelScope 中 launch suspend 调用

=== BUG-004 复验: UI favoriteIds 引用 ===
  文件: ui/SearchScreen.kt + ui/PlayerScreen.kt
  ✓ 两处引用 viewModel.favoriteIds 有效

=== 前端工程师额外修改 (CHG-105) 复验 ===
  文件: player/MusicService.kt
  ✓ 已删除（由 MediaPlaybackService 替代）

  文件: AndroidManifest.xml
  ✓ 重复权限已移除（4 个唯一声明）
  ✓ 仅保留 MediaPlaybackService（MusicService 声明已移除）

  文件: ui/FavoritesViewModel.kt
  ✓ toggleFavorite 包裹在 viewModelScope.launch（适配 suspend API）

  文件: data/local/TypeConverters.kt
  ✓ 类更名为 MusicTypeConverters（AppDatabase @TypeConverters 同步更新）

  ✓ FavoritesRepository.updateFavoriteStatus 使用 scope.launch 包裹 suspend 调用

=== 架构一致性验证 ===
  ✓ 单一数据源: Room (AppDatabase + FavoriteDao → FavoritesRepository)
  ✓ 单一 MediaSession 服务: MediaPlaybackService（删除冗余 MusicService）
  ✓ StateFlow 驱动: favoriteIds / favoriteSongs 实时响应 Room 变更
  ✓ MVVM: ViewModel → FavoritesRepository (object) → Room → StateFlow
  ✓ MusicPlayerManager.init() 不创建 ExoPlayer，启动 Service 后由 bindPlayer() 绑定
```

报告 TR-2026-012 (迭代12 全线验收):

=== TASK-BE-301: SearchViewModel source 过滤 ===
  文件: ui/SearchViewModel.kt:76-77
  ✓ results.filter { it.source == platform.value } 已添加
  ✓ 注释 AD-009 标注客户端侧防御性过滤原因
  ✓ kuwo 过滤: 仅保留 source=kuwo
  ✓ kugou 过滤: 仅保留 source=kugou
  ✓ migu 过滤: 仅保留 source=migu
  ✓ netease 过滤: 仅保留 source=netease
  ✓ 空结果列表无崩溃
  ✓ 过滤保持原始排序
  ✓ null source 正确处理

=== TASK-BE-302: NeteaseViewModel 歌单降级加固 ===
  文件: ui/netease/NeteaseViewModel.kt:218-219
  ✓ AD-008 降级注释清晰标注在 loadUserPlaylists 上方
  ✓ loadUserPlaylists() 强制调用 api()（代理），无 useDirect 检查
  ✓ loadPlaylistTracks() 强制调用 api()（代理），无 useDirect 检查
  ✓ loadDailyRecommend() 保留双模式（注释 + useDirect 分支）
  ✓ login() 保留双模式
  ✓ loadPersonalized() 保留双模式
  ✓ playTrack() 保留双模式（songUrl 获取）
  ✓ toggleDirectMode() 逻辑不变
  ✓ logout() 正确处理 directMode 分支

=== TASK-BE-303: Docker 部署文件 ===
  文件: deploy/Dockerfile, deploy/docker-compose.yml, deploy/nginx.conf
  ✓ Dockerfile: node:20-alpine 基础镜像
  ✓ Dockerfile: git clone NeteaseCloudMusicApi, npm install --production
  ✓ Dockerfile: EXPOSE 3000, CMD node app.js
  ✓ docker-compose.yml: 单服务编排, restart unless-stopped
  ✓ docker-compose.yml: healthcheck curl login/status
  ✓ docker-compose.yml: 512M memory limit
  ✓ nginx.conf: proxy_pass http://127.0.0.1:3000
  ✓ nginx.conf: proxy headers (Host/Real-IP/Forwarded-For/Proto)
  ✓ nginx.conf: /health endpoint
  ⚠️ deploy/README.md 未找到（建议补充部署步骤文档）

=== TASK-FE-301: LoginSettingsScreen UI 更新 ===
  文件: data/settings/NeteaseSettings.kt, ui/netease/NeteaseScreen.kt, ui/netease/NeteaseViewModel.kt
  ✓ NeteaseSettings.DEFAULT_API_URL = "https://your-netease-api.example.com"
  ✓ API URL placeholder 引用 DEFAULT_API_URL
  ✓ 新增「测试连接」Refresh 图标按钮
  ✓ 连接测试: loading indicator 显示/隐藏
  ✓ 连接成功: ✅ 连接成功 (primary 颜色)
  ✓ 连接失败: ❌ 具体错误信息 (ConnectException/Timeout/UnknownHost/通用)
  ✓ testProxyConnection() 5s 超时, HEAD 请求
  ✓ 直连/代理模式说明文字区分
  ✓ 所有新增 API 无破坏性变更

---

## 反馈产品经理

> ⚠️ **测试完成后，更新 `产品经理.md` §待验收 区域**

更新格式:
```
| ID | 需求 | 测试状态 (✅/❌) | 验收决定 (由 PM 填写) |
```

---

## 测试策略参考

### 分层测试金字塔

```
         ┌──────┐
         │ E2E  │  ← 少量端到端（核心流程）
        ┌┴──────┴┐
        │ 集成测试 │  ← API 集成、播放器集成
       ┌┴────────┴┐
       │  UI 测试   │  ← Compose 组件交互
      ┌┴──────────┴┐
      │   单元测试   │  ← ViewModel、Util、Model
      └────────────┘
```

### 测试框架
| 类型 | 框架 |
|------|------|
| 单元测试 | JUnit 4.13.2 |
| Android 测试 | AndroidX JUnit 1.1.5 |
| Compose UI | Compose UI Test JUnit4 |
| 端到端 | Espresso 3.5.1 |

### 测试目录
```
app/src/test/          → 单元测试（JVM）
app/src/androidTest/   → 仪器化测试（Android 环境）
```

⚠️ 当前测试目录仅包含模板文件，无实际测试代码。

---

## 待编写测试总览

| ID | 测试对象 | 类型 | 优先级 |
|----|---------|------|--------|
| UT-01 | SearchViewModel | 单元 | P0 |
| UT-02 | MusicPlayerManager | 单元 | P0 |
| UT-03 | NeteaseViewModel | 单元 | P1 |
| UT-04 | Song Model 解析 | 单元 | P1 |
| UT-05 | NeteaseSettings | 单元 | P2 |
| UI-01 | SearchScreen 搜索交互 | UI | P1 |
| UI-02 | PlayerScreen 播放控制 | UI | P1 |
| UI-03 | NeteaseScreen 登录流程 | UI | P2 |
| IT-01 | MusicApiService 连通 | 集成 | P1 |
| IT-02 | 搜索→播放完整流程 | E2E | P2 |

---

## 最后更新
2026-05-15 — 迭代12：后端3项任务已交付（BUG-005 source过滤 / F15降级审计 / F16 Docker部署）；前端 TASK-FE-301（设置页API URL + 连接测试）已交付。共4项测试任务待验证
