# 架构师 (Architect)

> 本文档供 AI 智能体 / IDE / CLI 在新建会话时直接读取。  
> **AI 指令**: 请以架构师身份工作。首先读取 `产品经理.md` 的「当前发布需求」区域。

---

## AI 记忆锚点（每次会话必读）

- 当前迭代: **14**（F13 深色模式 + F14 定时关闭）
- 架构: MVVM + StateFlow，无 DI 框架，手动导航，object 单例
- **重要**: 分析需求前先读 `技术债务.md`，避免在债务区域引入新复杂度
- **重要**: 如有架构变更，必须创建 ADR 文件（见下方 ADR 规范）

---

## 角色定位

分析需求对系统架构的影响，拆解为前后端开发任务，确保技术方案与现有架构一致。

---

## 职责

1. 读取 `产品经理.md` 的「当前发布需求」
2. **读取 `技术债务.md`**，评估需求是否关联现有债务
3. 分析需求对现有架构的影响
4. 将需求拆解为前端任务和后端任务
5. 分别写入 `前端工程师.md` 和 `后端工程师.md` 的「当前任务」区域
6. 若有架构变更需求，创建 ADR 文件

---

## 工作流协议

```
              ← 读取 §当前发布需求              产出任务 →
  产品经理 ───────────────── 架构师 ───────────────────── 前端工程师
                             │───────────────────── 后端工程师
                             │  (写 §当前任务)       (写 §当前任务)
                             │
                             ├── 读取 `技术债务.md`
                             └── 创建 ADR（如需）
```

---

## 当前输入

> 📥 **从 `产品经理.md` 读取** — 需分析的需求

| 来源 | 需求 ID | 需求说明 |
|------|---------|---------|
| 产品经理 | F14 | 定时关闭 — 倒计时到时自动暂停播放 |
| 产品经理 | F13 | 深色模式 — 切换 App 亮色/暗色主题 |

---

## 需求分析

> ⚠️ **架构师在此分析需求对架构的影响**
>
> 分析前必读:
> 1. `产品经理.md` §当前发布需求（了解需求详情和验收标准）
> 2. `技术债务.md` §活跃债务（评估是否关联或解决债务）
> 3. `技术规范.md`（确保方案符合规范）

### F14: 定时关闭

**背景:**
- 用户希望在指定时间后自动暂停播放
- 需要倒计时 UI 和播放暂停联动

**现有架构相关模块:**
- `ui/SearchScreen.kt` — 需新增定时关闭入口按钮
- `ui/SearchViewModel.kt` — 需新增倒计时逻辑
- `player/MusicPlayerManager.kt` — 暂停播放接口已存在
- `data/settings/NeteaseSettings.kt` — 可复用存储时间偏好

**技术债务关联:**
- TD-004: `SearchViewModel` 职责过重 — 本次新增定时逻辑会加剧此问题，建议在任务分配时说明"本迭代允许临时在 SearchViewModel 中添加，迭代 15 必须拆分为 TimerViewModel"

**分析:**
```
需求 F14:
- 涉及模块: UI 层（入口按钮 + 弹窗选择时间 + 倒计时展示） + ViewModel 层（倒计时协程 + 暂停播放）
- 是否新增模块: 否 — 在现有模块中扩展
- 是否需要新依赖: 否 — 使用 Kotlin Coroutines 倒计时
- 架构风险: 低 — 倒计时逻辑简单，与播放器解耦
- 与现有架构的一致性: 一致 — MVVM + StateFlow
- 修改点: SearchScreen.kt, SearchViewModel.kt, MusicPlayerManager.kt
```

### F13: 深色模式

**背景:**
- 当前 Theme.kt 已支持 Android 12+ 动态颜色（跟随系统）
- 新增手动切换亮色/暗色主题的能力

**现有架构相关模块:**
- `ui/theme/Theme.kt` — 当前仅 check SystemTheme，需新增自定义主题切换
- `ui/theme/Color.kt` — 当前仅定义亮色颜色，需补充暗色颜色
- `ui/SearchScreen.kt` — 需新增主题切换入口
- `data/settings/NeteaseSettings.kt` — 可复用存储主题偏好

**技术债务关联:**
- TD-003: `NeteaseSettings` 职责混杂 — 本次主题偏好添加会进一步加剧，建议本迭代同步拆分 `NeteaseSettings` 为 `NeteaseSettings`（认证）+ `AppSettings`（通用）

**分析:**
```
需求 F13:
- 涉及模块: UI 层（主题切换入口） + Theme 层（动态切换亮/暗色） + 设置层（持久化主题偏好）
- 是否新增模块: 是 — 新增 AppSettings（拆分自 NeteaseSettings）
- 是否需要新依赖: 否
- 架构风险: 低 — Compose 主题切换机制成熟
- 与现有架构的一致性: 一致 — 在现有 Theme.kt 基础上扩展
- 修改点: Theme.kt, Color.kt, SearchScreen.kt, NeteaseSettings.kt（拆分）
```

---

### 影响总结

| 需求 | 新增模块 | 删除模块 | 新依赖 | 风险 | 架构调整 | 关联债务 |
|------|---------|---------|--------|------|---------|---------|
| F14 | 0 | 0 | 无 | 低 | 在现有模块扩展 | TD-004（临时加剧） |
| F13 | 1 (AppSettings) | 0 | 无 | 低 | Theme 模块扩展 + 设置拆分 | TD-003（本次解决） |

---

## 架构决策记录 (ADR)

> **ADR 规范**: 每个架构决策创建独立文件 `架构决策/AD-{序号}-{简短描述}.md`
>
> 文件模板:
> ```markdown
> # AD-{序号}: {决策标题}
> **日期**: YYYY-MM-DD
> **状态**: 已接受 / 已废弃 / 已替代
> **决策**: {一句话描述}
> **原因**: {为什么做这个决策}
> **影响**: {对现有代码的影响}
> **替代方案**: {考虑过但放弃的替代方案}
> ```

### 现有 ADR 列表

| ID | 文件 | 决策 | 状态 |
|----|------|------|------|
| AD-001 | `架构决策/AD-001-无DI框架.md` | MVP 阶段不引入 DI 框架 | 已接受 |
| AD-002 | `架构决策/AD-002-Room取代DataStore.md` | F10 收藏持久化选用 Room | 已接受 |
| AD-003 | `架构决策/AD-003-MediaSessionService.md` | F12 后台播放使用 MediaSessionService | 已接受 |
| AD-006 | `架构决策/AD-006-Room单一数据源.md` | FavoritesRepository 统一 Room 单一数据源 | 已接受 |
| AD-007 | `架构决策/AD-007-单例模式.md` | FavoritesRepository 保持 object 单例 | 已接受 |
| AD-008 | `架构决策/AD-008-歌单降级代理.md` | F15 歌单强制走代理 | 已接受 |
| AD-009 | `架构决策/AD-009-客户端Source过滤.md` | BUG-005 客户端侧 source 过滤 | 已接受 |
| AD-010 | `架构决策/AD-010-移除直连模式.md` | 移除 F15 直连，统一代理模式 | 已接受 |
| AD-011 | `架构决策/AD-011-心动模式独立模块.md` | 心动模式作为独立功能模块 | 已接受 |

> **注意**: 迭代 14 的 AD-012（如有）创建后追加到上表。

---

## 任务分配

> ⚠️ **架构师拆解后，请将任务写入对应工程师文件**

### 任务 ID 分配规则
格式: `TASK-{角色}-{序号}`
- 前端: `TASK-FE-{500+}`（迭代 14 使用 501+）
- 后端: `TASK-BE-{500+}`（迭代 14 使用 501+）

### → 前端工程师

更新 `前端工程师.md` §当前任务:

```
TASK-FE-501 | 需求 F14 | 定时关闭 UI（按钮/弹窗/倒计时展示）+ 倒计时逻辑 | 验收标准: 按钮可见/弹窗有15-30-60选项/到时暂停/可取消
TASK-FE-502 | 需求 F13 | 深色模式切换 UI + Theme 动态切换 | 验收标准: 按钮可见/主题正确应用/偏好持久化/跟随系统正常
```

### → 后端工程师

更新 `后端工程师.md` §当前任务:

```
TASK-BE-501 | 需求 F14 | 定时关闭时间偏好持久化 + Timer 协程管理 | 验收标准: 偏好正确持久化/重启可读取/倒计时到期可靠触发暂停
TASK-BE-502 | 需求 F13 | 主题偏好持久化 + Color.kt 暗色定义 + NeteaseSettings 拆分为 AppSettings | 验收标准: 偏好持久化/暗色配色完整/Theme.kt 正确响应/设置拆分无回归
```

---

## 迭代 14 任务总览

| 任务 ID | 需求 | 分配 | 描述 | 状态 | 关联债务 |
|---------|------|------|------|------|---------|
| TASK-BE-501 | F14 | 后端 | 定时关闭偏好持久化 + Timer 协程 | 🔄 待开发 | TD-004（临时） |
| TASK-BE-502 | F13 | 后端 | 主题偏好 + 暗色定义 + 设置拆分 | 🔄 待开发 | TD-003（解决） |
| TASK-FE-501 | F14 | 前端 | 定时关闭 UI + 倒计时逻辑 | 🔄 待开发 | — |
| TASK-FE-502 | F13 | 前端 | 深色模式 UI + Theme 切换 | 🔄 待开发 | — |

---

## 交叉审核分配（迭代 14 启用）

| 任务 | 交叉审核人 | 审核重点 |
|------|-----------|---------|
| TASK-FE-502 (深色模式) | 后端工程师 | 确认 Theme.kt 的暗色方案 Material 3 合规 |
| TASK-BE-501 (定时关闭) | 前端工程师 | 确认协程作用域和生命周期管理正确 |

---

## 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 语言 | Kotlin | 2.2.10 |
| UI | Jetpack Compose + Material 3 | BOM 2026.02.01 |
| 构建 | Gradle Kotlin DSL + Version Catalog | AGP 9.2.0 |
| 网络 | Retrofit + OkHttp + Gson | 2.11 / 4.12 |
| 图片 | Coil Compose | 2.7.0 |
| 播放 | Media3 ExoPlayer | 1.5.1 |
| 异步 | Kotlin Coroutines + Flow | 1.8.0 |
| 持久化 | Room + DataStore Preferences | — |

---

## 架构概要

```
UI Layer (Compose) → ViewModel (StateFlow) → Data Layer (Retrofit / Room / ExoPlayer)
```

- 单 `MainActivity` + 手动状态导航
- Kotlin `object` 单例（无 DI 框架）
- 详见 `项目进度.md` 完整架构

---

## 最后更新

2026-05-29 — 迭代 14 任务分配，启用 ADR 独立文件、交叉审核、技术债务关联
