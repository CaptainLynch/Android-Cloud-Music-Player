# 前端工程师 (Frontend Engineer — Android Compose UI)

> 本文档供 AI 智能体 / IDE / CLI 在新建会话时直接读取。  
> **AI 指令**: 请以前端工程师身份工作。首先读取 `架构师.md` 中分配给前端的「当前任务」。

---

## AI 记忆锚点（每次会话必读）

- 当前迭代: **14**（TASK-FE-501 定时关闭 UI / TASK-FE-502 深色模式 UI）
- 技术栈: Compose + Material3 / Coil / StateFlow
- **编码前必读**: `技术规范.md`（Compose 编码铁律、命名规范）
- **约束**: 纯 Compose，无 XML，无 DI，MVVM + StateFlow

---

## 角色定位

负责 Android 客户端 Compose UI 开发，包括页面组件、主题样式、交互体验、UI 状态管理。

---

## 职责

1. 读取本文件「当前任务」区域
2. **编码前读取 `技术规范.md`** 确保符合规范
3. 根据任务描述实现 Compose UI 代码
4. 完成后更新「代码变更」记录
5. 触发代码审核（自审 + **交叉审核**）
6. 审核通过后更新 `测试工程师.md` 的「当前测试任务」
7. **同步更新相关文档**（见下方文档同步检查清单）

---

## 工作流协议

```
            ← 读取 §当前任务       完成编码 ──→ 代码审核 ──→ 通知测试
  架构师 ───────────────── 前端工程师 ──────────────────── 测试工程师
          (架构师写入任务)           (写 §代码变更)  (写 §审核状态)  (写测试工程师 §当前测试任务)
                                           ↑
                                           ├── 自审检查清单
                                           └── 交叉审核（如分配）
```

---

## 当前任务

> 📥 **迭代 14** — 从架构师接收的任务

| 任务 ID | 来源需求 | 任务描述 | 优先级 | 状态 |
|---------|---------|---------|--------|------|
| TASK-FE-501 | F14 | 定时关闭 UI（按钮/弹窗/倒计时展示）+ 倒计时逻辑 | P4 | 🔄 待开发 |
| TASK-FE-502 | F13 | 深色模式切换 UI + Theme 动态切换 | P3 | 🔄 待开发 |

### 任务详情

---

**TASK-FE-501: 定时关闭 UI + 倒计时逻辑**

- 涉及文件: `ui/SearchScreen.kt`, `ui/SearchViewModel.kt`, `player/MusicPlayerManager.kt`
- 新增文件: 无
- UI 组件: SearchScreen 顶栏添加定时关闭按钮；点击弹出时间选择弹窗（15min/30min/60min/自定义）；播放时显示倒计时
- 状态管理: SearchViewModel 新增 `remainingTime: StateFlow<Int>` 和定时器协程
- 依赖: 无新依赖（Kotlin Coroutines 倒计时）
- 预计影响: 低
- **技术债务关联**: TD-004（SearchViewModel 职责过重）— 本次允许临时添加，但代码注释需标记 `// TODO: TD-004 迭代15拆分为 TimerViewModel`

**实施要点:**
1. SearchScreen 顶栏添加定时器图标按钮
2. 点击弹出 AlertDialog 选择时间
3. SearchViewModel 启动倒计时协程，到期调用 MusicPlayerManager.pause()
4. 倒计时期间在 MiniPlayerBar 或顶栏显示剩余时间

**验收标准:**
- [ ] 定时关闭按钮可见可点击
- [ ] 弹窗提供 15/30/60 分钟选项
- [ ] 倒计时结束后自动暂停播放
- [ ] 再次点击可取消定时

---

**TASK-FE-502: 深色模式切换 UI + Theme 动态切换**

- 涉及文件: `ui/theme/Theme.kt`, `ui/theme/Color.kt`, `ui/SearchScreen.kt`
- 新增文件: 无
- UI 组件: SearchScreen 顶栏添加主题切换按钮（亮色/暗色/跟随系统）
- 状态管理: 通过 Compose 的 `isSystemInDarkTheme()` + 手动覆盖实现
- 依赖: 无新依赖
- 预计影响: 中 — 需定义暗色配色方案

**实施要点:**
1. Color.kt 补充 `DarkColorScheme`
2. Theme.kt 新增 `AppTheme(darkTheme: Boolean?)` 参数支持手动切换
3. MainActivity 持久化主题偏好，传入 Theme
4. SearchScreen 顶栏添加主题切换图标，点击切换

**验收标准:**
- [ ] 切换按钮可见可点击
- [ ] 亮色/暗色主题正确应用到所有页面
- [ ] 主题偏好持久化，重启后保持

---

## 实施计划

> ⚠️ **前端工程师在此编写实施计划**

模板:
```
TASK-XXX: [任务名称]
- 涉及文件: [列出]
- 新增文件: [列出]
- UI 组件: [列出需新建的 Composable]
- 状态管理: [ViewModel / StateFlow 变更]
- 依赖: [是否需新依赖]
- 预计影响: [低/中/高]
- 债务关联: [如有，引用 TD-xxx]
```

---

## 代码变更

> ⚠️ **每次完成编码后记录**

| 变更 ID | 任务 | 文件 | 类型 | 状态 |
|---------|------|------|------|------|
| CHG-FE-101 | TASK-FE-101 | `ui/SearchScreen.kt`（SongItem 重构 + 收藏按钮）、`ui/PlayerScreen.kt`（顶栏收藏按钮）、`ui/SearchViewModel.kt`（集成 FavoritesRepository） | 修改 | ✅ |
| CHG-FE-102 | TASK-FE-102 | 新增 `ui/FavoritesScreen.kt`、`ui/FavoritesViewModel.kt`、`data/repository/FavoritesRepository.kt`；修改 `MainActivity.kt`（导航）、`SearchScreen.kt`（入口按钮） | 新增+修改 | ✅ |
| CHG-FE-103 | TASK-FE-103 | 新增 `player/MusicService.kt`（前台 Service + MediaSession + 通知栏）；修改 `AndroidManifest.xml`（权限+Service声明）、`player/MusicPlayerManager.kt`（服务启停+播放控制）、`MainActivity.kt`（Intent 处理） | 新增+修改 | ✅ |
| CHG-FE-104 | TASK-FE-104 | `ui/netease/NeteaseScreen.kt`（LoginSettingsScreen 新增 Switch + 确认弹窗） | 修改 | ✅ |
| CHG-FE-FIX-01 | TASK-FE-FIX-01 | 删除 `player/MusicService.kt`（被 MediaPlaybackService 替代）；修正 `AndroidManifest.xml`（去重权限+移除冗余Service）；修正 `ui/FavoritesViewModel.kt`（适配 suspend toggleFavorite）；修正 `data/local/TypeConverters.kt`→`MusicTypeConverters`；修正 `player/MediaPlaybackService.kt`（通知Intent指向播放器） | 修改 | ✅ |
| CHG-FE-301 | TASK-FE-301 | 修改 `data/settings/NeteaseSettings.kt`（默认API URL更新）、`ui/netease/NeteaseScreen.kt`（LoginSettingsScreen：连接测试按钮+状态指示器+直连模式提示+模式说明）、`ui/netease/NeteaseViewModel.kt`（新增 testProxyConnection） | 修改 | ✅ |
| CHG-FE-401 | TASK-FE-401 | 移除直连/代理切换 UI | 修改 | ✅ |
| CHG-FE-402 | TASK-FE-402 | 心动模式/智能播放 UI 实现 | 新增+修改 | ✅ |
| **CHG-FE-501** | **TASK-FE-501** | **定时关闭 UI + 倒计时逻辑** | **待开发** | 🔄 |
| **CHG-FE-502** | **TASK-FE-502** | **深色模式切换 UI + Theme 动态切换** | **待开发** | 🔄 |

---

## 代码审核

> ⚠️ **编码完成后进行自审 + 交叉审核**

### 自审检查清单

- [ ] 新增代码符合 MVVM 模式
- [ ] 使用 StateFlow 而非 LiveData
- [ ] 无 XML 布局，全部 Compose
- [ ] Compose 使用 `collectAsState()` 订阅状态
- [ ] 无硬编码字符串（strings.xml 或 Compose stringResource）
- [ ] UI 与 ViewModel 解耦
- [ ] Coil 异步图片加载
- [ ] 无内存泄漏（Effect / DisposableEffect 正确使用）
- [ ] 符合 `技术规范.md` 命名规范
- [ ] 如涉及架构变更，ADR 已创建或更新

### 交叉审核（迭代 14）

| 任务 | 交叉审核人 | 审核重点 | 状态 |
|------|-----------|---------|------|
| TASK-FE-502 (深色模式) | 后端工程师 | 确认 Theme.kt 的暗色方案 Material 3 合规 | ⏳ 待审核 |

### 审核结论模板

```markdown
审核结论: [x] 通过 / [ ] 不通过
审核说明: [关键发现和确认项]
```

---

## 文档同步检查清单

> ⚠️ **代码变更完成后必须执行** — 确保文档与代码同步

- [ ] `前端工程师.md` — 代码变更表已更新
- [ ] `项目进度.md` — 如迭代状态变化，已更新工作流状态
- [ ] `技术债务.md` — 如代码中标记了 TODO/TD-xxx，已确认债务表中存在对应条目
- [ ] `架构师.md` — 如涉及架构变更，ADR 已创建
- [ ] `产品经理.md` — 如功能清单有变化，已同步更新

---

## 通知测试工程师

> ⚠️ **代码审核通过后，更新 `测试工程师.md` §当前测试任务**

需告知测试工程师:
- 变更文件列表
- 新增 UI 交互点
- 关键测试场景
- 已标记的技术债务 TODO（测试需关注）

---

## UI 页面结构

```
MainActivity
├── SearchScreen (搜索主页)
│   ├── 搜索栏 (TextField + FilterChip)
│   ├── 结果列表 (LazyColumn)
│   ├── 定时关闭按钮 → 弹窗 (F14 新增)
│   ├── 主题切换按钮 (F13 新增)
│   └── 歌单入口按钮 → NeteaseScreen
├── PlayerScreen (全屏播放器)
│   ├── 封面图 / 歌曲信息 / SeekBar
│   ├── 播放控制 / 歌词预览
│   └── 队列管理
└── NeteaseScreen (网易云)
    ├── 登录 / 用户信息
    ├── 歌单列表 / 每日推荐
    └── 歌单详情
```

---

## 待开发池（供后续迭代参考）

| 任务 | 描述 | 优先级 |
|------|------|--------|
| UI-01 | 收藏/喜欢按钮 UI + 收藏列表 | P2 |
| UI-02 | 播放历史页面 | P3 |
| UI-03 | 通知栏媒体控制 | P2 |
| UI-04 | 深色模式开关 | P3 → 迭代 14 处理 |
| UI-05 | 歌词全屏模式 | P3 |
| UI-06 | 定时关闭弹窗 | P4 → 迭代 14 处理 |

---

## 最后更新

2026-05-29 — 迭代 14 启动，新增文档同步检查清单、交叉审核、技术债务关联、AI 记忆锚点
