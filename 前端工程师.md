# 前端工程师 (Frontend Engineer — Android Compose UI)

> 本文档供 AI 智能体 / IDE / CLI 在新建会话时直接读取。  
> **AI 指令**: 请以前端工程师身份工作。首先读取 `架构师.md` 中分配给前端的「当前任务」。

---

## 角色定位
负责 Android 客户端 Compose UI 开发，包括页面组件、主题样式、交互体验、UI 状态管理。

## 职责
1. 读取本文件「当前任务」区域
2. 根据任务描述实现 Compose UI 代码
3. 完成后更新「代码变更」记录
4. 触发代码审核（自我检查或人工审查）
5. 审核通过后更新 `测试工程师.md` 的「当前测试任务」

---

## 工作流协议

```
            ← 读取 §当前任务       完成编码 ──→ 代码审核 ──→ 通知测试
  架构师 ───────────────── 前端工程师 ──────────────────── 测试工程师
          (架构师写入任务)           (写 §代码变更)  (写 §审核状态)  (写测试工程师 §当前测试任务)
```

---

## 当前任务

> 📥 **F10 修正需求** — 测试验收驳回，4 个编译错误需修复

| 任务 ID | 来源需求 | 任务描述 | 优先级 | 状态 |
|---------|---------|---------|--------|------|
| TASK-FE-FIX-01 | F10 修正 | 修复 SearchScreen/PlayerScreen 中 `viewModel.favoriteIds` 引用 (BUG-004) | P0 | ⏳ |

### 任务详情

---

**TASK-FE-FIX-01: 修复 SearchScreen/PlayerScreen favoriteIds 引用**

- 涉及文件: `ui/SearchScreen.kt`（第50行）、`ui/PlayerScreen.kt`（第42行）
- 问题: 两处均引用 `viewModel.favoriteIds.collectAsState()`，但 SearchViewModel 当前未暴露 `favoriteIds` 字段
- 方案: 后端修复 SearchViewModel 后会暴露 `favoriteIds: StateFlow<Set<Long>>`，前端代码无需改动；验证编译通过即可
- 如后端选择其他暴露方式（如直接使用 `FavoritesRepository.favoriteIds`），前端需同步调整

**验收标准:**
- [ ] SearchScreen 代码引用 `viewModel.favoriteIds` 编译无报错
- [ ] PlayerScreen 代码引用 `viewModel.favoriteIds` 编译无报错
- [ ] 项目整体 `./gradlew assembleDebug` 通过

---

> 注: F10 原 4 个功能任务（TASK-FE-101 ~ 104）的 UI 实现在测试中已验证通过，仅需修正编译错误。详见 `后端工程师.md` 对应的数据层修复。

---

## 实施计划

> ⚠️ **前端工程师在此编写实施计划**

模板:
```
TASK-XXX: [任务名称]
- 涉及文件: [列出]
- 新增文件: [列出]
- UI 组件: [列出需新建的 Composable]
- 状态管理: [ViewModel / StateFlow 变更]
- 依赖: [是否需新依赖]
- 预计影响: [低/中/高]
```

---

## 代码变更

> ⚠️ **每次完成编码后记录**

| 变更 ID | 任务 | 文件 | 类型 | 状态 |
|---------|------|------|------|------|
| CHG-101 | TASK-FE-101 | ui/SearchScreen.kt（SongItem 重构 + 收藏按钮）、ui/PlayerScreen.kt（顶栏收藏按钮）、ui/SearchViewModel.kt（集成 FavoritesRepository） | 修改 | ✅ |
| CHG-102 | TASK-FE-102 | 新增 ui/FavoritesScreen.kt、ui/FavoritesViewModel.kt、data/repository/FavoritesRepository.kt；修改 MainActivity.kt（导航）、SearchScreen.kt（入口按钮） | 新增+修改 | ✅ |
| CHG-103 | TASK-FE-103 | 新增 player/MusicService.kt（前台 Service + MediaSession + 通知栏）；修改 AndroidManifest.xml（权限+Service声明）、player/MusicPlayerManager.kt（服务启停+播放控制）、MainActivity.kt（Intent 处理） | 新增+修改 | ✅ |
| CHG-104 | TASK-FE-104 | ui/netease/NeteaseScreen.kt（LoginSettingsScreen 新增 Switch + 确认弹窗） | 修改 | ✅ |
| CHG-105 | TASK-FE-FIX-01 | 删除 player/MusicService.kt（被 MediaPlaybackService 替代）；修正 AndroidManifest.xml（去重权限+移除冗余Service）；修正 ui/FavoritesViewModel.kt（适配 suspend toggleFavorite）；修正 data/local/TypeConverters.kt→MusicTypeConverters；修正 player/MediaPlaybackService.kt（通知Intent指向播放器） | 修改 | ✅ |

---

## 代码审核

> ⚠️ **编码完成后进行自审**

审核检查清单:
- [x] 新增代码符合 MVVM 模式
- [x] 使用 StateFlow 而非 LiveData
- [x] 无 XML 布局，全部 Compose
- [x] Compose 使用 `collectAsState()` 订阅状态
- [x] 无硬编码字符串（strings.xml 或 Compose stringResource）
- [x] UI 与 ViewModel 解耦
- [x] Coil 异步图片加载
- [x] 无内存泄漏（Effect / DisposableEffect 正确使用）

审核结论: [x] 通过

审核说明:
- FavoritesRepository 已由后端统一为 Room 单一数据源（移除 SharedPreferences 持久化），前端 FavoritesScreen/SearchViewModel/FavoritesViewModel 已适配
- SongItem 已重构为纯 UI 组件（接收回调参数），与 ViewModel 解耦，可在 SearchScreen 和 FavoritesScreen 中复用
- 通知栏/锁屏控制由后端 MediaPlaybackService（Media3）接管，前端已删除冗余 MusicService.kt，并在 MediaPlaybackService 中补充通知点击→播放器 Intent
- TypeConverters 重命名为 MusicTypeConverters（避免与 Room @TypeConverters 注解名冲突）
- directMode 切换含确认弹窗 + 自动登出 + 清空 cookie，符合验收要求

---

## 通知测试工程师

> ⚠️ **代码审核通过后，更新 `测试工程师.md` §当前测试任务**

需告知测试工程师:
- 变更文件列表
- 新增 UI 交互点
- 关键测试场景

---

## UI 页面结构

```
MainActivity
├── SearchScreen (搜索主页)
│   ├── 搜索栏 (TextField + FilterChip)
│   ├── 结果列表 (LazyColumn)
│   └── 歌单入口按钮 → NeteaseScreen
├── PlayerScreen (全屏播放器)
│   ├── 封面图 / 歌曲信息 / SeekBar
│   ├── 播放控制 / 歌词预览
│   └── 队列管理
└── NeteaseScreen (网易云)
    ├── 登录 / 用户信息
    ├── 歌单列表 / 每日推荐
    └── 歌单详情
```

---

## 待开发池（供后续迭代参考）

| 任务 | 描述 | 优先级 |
|------|------|--------|
| UI-01 | 收藏/喜欢按钮 UI + 收藏列表 | P2 |
| UI-02 | 播放历史页面 | P3 |
| UI-03 | 通知栏媒体控制 | P2 |
| UI-04 | 深色模式开关 | P3 |
| UI-05 | 歌词全屏模式 | P3 |
| UI-06 | 定时关闭弹窗 | P4 |

---

## 最后更新
2026-05-14 — 前端工程师已完成迭代11全部任务 + F10修正（TASK-FE-FIX-01），通过协调自审
