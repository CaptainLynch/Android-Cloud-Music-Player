# 工作流操作指南 (VIBE CODE)

> 本文档描述如何通过双终端并行实现文件驱动协作。  
> 所有角色间的通信通过读写 Markdown 文件完成。

---

## 工作流总览

```
+-------------------------------------------------------------+
|                    你的两个终端                               |
+---------------------------+---------------------------------+
|   终端 A (全局 TUI)       |     终端 B (开发 TUI)           |
|   模型: Gemini (固定)     |     模型: 动态切换               |
|   当前: antigravity cli   |     当前: claude code           |
|   可换: opencode/codex    |     可换: opencode/codex        |
|                           |     切换: CC-switch /配置切换     |
+---------------------------+---------------------------------+
|  产品经理                 |  前端工程师                     |
|  架构师                   |  后端工程师                     |
|  测试工程师               |                                 |
|                           |  切换命令:                      |
|  角色特点: 低频调用        |  /switch kimi                   |
|  每迭代 2~5 次            |  /switch deepseek-v4-pro        |
|  需要大上下文              |  /switch v2.5-pro               |
|                           |  /switch v4-flash               |
|                           |  /switch mimos                  |
+---------------------------+---------------------------------+
          |                            |
          +------------+---------------+
                       |
              文件驱动协作（MD 文件读写）
```

---

## 双终端操作模型

> 本项目基于**双终端并行**模式运作：
> - **终端 A（全局 TUI）**: 固定跑 **Gemini** 模型，负责低频全局角色
>   - *你当前的工具*: `antigravity cli`
> - **终端 B（开发 TUI）**: 动态切换非 Gemini 模型，负责高频开发角色
>   - *你当前的工具*: `claude code + CC-switch`
>   - *支持的其他工具*: `opencode`, `codex`, `aider` 等任何支持模型切换的 TUI

### 为什么这样分配？

| 维度 | 终端 A (Gemini) | 终端 B (开发 TUI) |
|------|-----------------|-------------------|
| **调用频率** | 低（每迭代 2~5 次） | 高（每迭代 10~30 次） |
| **上下文需求** | 极大（需载入全项目文件） | 中等（聚焦具体代码文件） |
| **推理复杂度** | 高（架构决策、需求分析） | 中高（代码实现、调试） |
| **模型切换需求** | 不需要（单一角色用 Gemini 足够） | 强烈（前后端需要不同模型） |
| **成本敏感** | 不敏感（调用少） | 敏感（调用频繁） |

**Gemini 的优势**: 1M~2M 超大上下文（适合载入全部项目文档做分析）、多模态（可以看 UI 截图）、成本低。

**终端 B 的优势**: 前后端开发需要最强推理模型（Kimi/DeepSeek 等），且需要根据任务快速切换模型。

---

## 并行工作流

### 典型迭代的时间线

```
时间轴 ---------------------------------------------------------->

终端 A (Gemini)
  |     [产品经理]
  |         |
  |     [架构师 ----------------]
  |                              |
  |                           [测试工程师 -------]
  |                                               |
  |                                           [产品经理验收]
  |
终端 B (开发 TUI)
  |              [后端工程师 ---------------]
  |              [前端工程师 ---------------------------]
  |              |                                       |
  |         /switch kimi                            /switch 切换
  |         或 /switch deepseek                    到另一个模型
  |
  |
  +------ 文件驱动协作（MD 文件读写交接）-------------------------/
```

### 五种并行组合

| 阶段 | 终端 A 角色 | 终端 B 角色 | 指令 |
|------|------------|------------|------|
| **需求->开发** | 产品经理发需求 | 后端先行开发 | A: 产品经理指令 / B: 后端指令 |
| **分析->开发** | 架构师分析 | 前端开始 UI | A: 架构师指令 / B: 前端指令 |
| **开发->开发** | 测试准备用例 | 前后端并行 | A: 测试准备指令 / B: 前端或后端 |
| **测试->修复** | 产品经理待命 | 后端修 Bug | A: 待命 / B: 后端修复指令 |
| **验收->回顾** | 产品经理验收+写回顾 | 空闲或预研 | A: 验收指令 / B: 空闲 |

> 终端 A 的角色切换是**手动切换**（同一个会话重新指定角色指令）。  
> 终端 B 的角色切换是**模型切换 + 角色指令**（如 `/switch kimi` 或 `--model kimi` 切模型后，输入新角色指令）。

---

## 终端 A 操作指令（全局 TUI -> Gemini）

> *你当前的工具*: `antigravity cli`
> *可替换为*: `opencode`, `codex`, `aider` 等任何支持文件上下文的 TUI

### 产品经理（发布需求）

```
读取 产品经理.md 和 项目进度.md。以产品经理身份发布下一轮需求，
使用需求模板（含验收标准），写入 产品经理.md 当前发布需求，
更新 项目进度.md 工作流状态。
```

### 产品经理（验收）

```
读取 产品经理.md 待验收 和 测试工程师.md 测试报告。
以产品经理身份验收，通过后更新 项目进度.md 并编写 迭代回顾.md。
```

### 架构师

```
读取 产品经理.md 当前发布需求、架构师.md、技术债务.md。
以架构师身份分析需求（标注债务关联），如需则创建 ADR，
分配任务到 前端工程师.md 和 后端工程师.md 当前任务，更新 项目进度.md。
```

### 测试工程师

```
读取 测试工程师.md 当前测试任务。以测试工程师身份编写和执行测试（含回归测试），
写测试报告并反馈到 产品经理.md 待验收。如发现架构级缺陷同步更新 技术债务.md。
```

---

## 终端 B 操作指令（开发 TUI -> 动态切换模型）

> *你当前的工具*: `claude code + CC-switch`
> *可替换为*: `opencode`（模型配置切换）, `codex`（--model 参数）等

### 后端工程师（/switch kimi 或 /switch deepseek）

```
读取 后端工程师.md 当前任务 和 技术规范.md。以后端工程师身份实现代码，
完成后更新 后端工程师.md 代码变更，自审+交叉审核后通知 测试工程师.md 当前测试任务，
执行文档同步检查清单。
```

### 前端工程师（/switch v2.5-pro 或 /switch kimi）

```
读取 前端工程师.md 当前任务 和 技术规范.md。以前端工程师身份实现代码，
完成后更新 前端工程师.md 代码变更，自审+交叉审核后通知 测试工程师.md 当前测试任务，
执行文档同步检查清单。
```

### 架构师（重分析时 /switch deepseek-v4-pro）

```
读取 产品经理.md 当前发布需求、架构师.md、技术债务.md。
以架构师身份分析需求（标注债务关联），如需则创建 ADR，
分配任务到 前端工程师.md 和 后端工程师.md 当前任务，更新 项目进度.md。
```

> 架构师通常在终端 A（Gemini）执行，但如果分析特别复杂（涉及大量代码），可以切到终端 B 用更强的模型。

---

## 模型切换时机速查

| 当前任务 | 推荐模型 | 切换命令 | 理由 |
|---------|---------|---------|------|
| 后端开发 | Kimi K2.6 | `/switch kimi` | 原生生态，Kotlin/协程最稳 |
| 前端开发 | V2.5-Pro | `/switch v2.5-pro` | MoE 效率高，Compose UI 质量好 |
| 复杂架构分析 | V4-Pro | `/switch deepseek-v4-pro` | 1.6T 参数，推理最强 |
| 简单修复/重构 | V4-Flash | `/switch v4-flash` | 极致性价比，响应快 |
| 超大量代码分析 | Gemini 3.1 Pro | 切到终端 A | 2M 上下文，一次载入全部 |
| 预研/文档整理 | MiMo | `/switch mimos` | 轻量快速，低成本 |

---

## 迭代回顾

每个迭代结束后，产品经理在**终端 A**编写 `迭代回顾.md`，记录:
- 做得好的（保持）
- 做得不好的（改进）
- 改进行动（下迭代执行）

---

## 快捷指令参考

以下是一行式指令，可直接粘贴到对应终端中启动各角色：

### 终端 A（全局 TUI -> Gemini）

**产品经理（发布需求）:**
```
读取 产品经理.md 和 项目进度.md。以产品经理身份发布下一轮需求，使用需求模板（含验收标准），写入 产品经理.md 当前发布需求，更新 项目进度.md 工作流状态。
```

**产品经理（验收）:**
```
读取 产品经理.md 待验收 和 测试工程师.md 测试报告。以产品经理身份验收，通过后更新 项目进度.md 并编写 迭代回顾.md。
```

**架构师:**
```
读取 产品经理.md 当前发布需求、架构师.md、技术债务.md。以架构师身份分析需求（标注债务关联），如需则创建 ADR，分配任务到 前端工程师.md 和 后端工程师.md 当前任务，更新 项目进度.md。
```

**测试工程师:**
```
读取 测试工程师.md 当前测试任务。以测试工程师身份编写和执行测试（含回归测试），写测试报告并反馈到 产品经理.md 待验收。如发现架构级缺陷同步更新 技术债务.md。
```

### 终端 B（开发 TUI -> 动态切换）

**后端工程师（先执行 /switch kimi）:**
```
读取 后端工程师.md 当前任务 和 技术规范.md。以后端工程师身份实现代码，完成后更新 后端工程师.md 代码变更，自审+交叉审核后通知 测试工程师.md 当前测试任务，执行文档同步检查清单。
```

**前端工程师（先执行 /switch v2.5-pro）:**
```
读取 前端工程师.md 当前任务 和 技术规范.md。以前端工程师身份实现代码，完成后更新 前端工程师.md 代码变更，自审+交叉审核后通知 测试工程师.md 当前测试任务，执行文档同步检查清单。
```

**复杂架构分析（先执行 /switch deepseek-v4-pro）:**
```
读取 产品经理.md 当前发布需求、架构师.md、技术债务.md。以架构师身份分析需求（标注债务关联），如需则创建 ADR，分配任务到 前端工程师.md 和 后端工程师.md 当前任务，更新 项目进度.md。
```

---

## 工作流规则

1. **单向流转**: 产品经理 -> 架构师 -> 前后端 -> 代码审核 -> 测试 -> 产品经理，不跳过中间角色
2. **文件即合约**: 每个角色的 当前任务 / 当前发布需求 / 当前测试任务 是交接协议
3. **状态同步**: 每完成一个 Stage，更新 `项目进度.md` 的「工作流状态」表
4. **前后端可并行**: 终端 B 前后端开发可以串行或并行（一个做完切另一个）
5. **一处写入，多处读取**: 架构师写入前后端任务后，前后端各自读取各自文件，互不干扰
6. **代码审核为自审 + 交叉审核**: 迭代 14 起启用交叉审核（架构师分配）
7. **迭代结束必须回顾**: 每个迭代结束后编写 `迭代回顾.md`
8. **技术债务持续追踪**: 任何角色发现债务随时更新 `技术债务.md`

---

## 文件驱动机制说明

每个角色的 MD 文件包含三个关键区域：

```
输入区域    — 由上游角色写入，本角色读取
工作区域    — 本角色在此执行工作，更新内容
输出区域    — 本角色写入，下游角色读取
```

具体映射：

| 上游 -> 下游 | 上游写入 | 下游读取 |
|-------------|---------|---------|
| 产品经理 -> 架构师 | `产品经理.md` 当前发布需求 | `架构师.md` 读取 |
| 架构师 -> 前端 | `前端工程师.md` 当前任务 | 同文件 当前任务 |
| 架构师 -> 后端 | `后端工程师.md` 当前任务 | 同文件 当前任务 |
| 前端 -> 测试 | `测试工程师.md` 当前测试任务 | 同文件 当前测试任务 |
| 后端 -> 测试 | `测试工程师.md` 当前测试任务 | 同文件 当前测试任务 |
| 测试 -> 产品经理 | `产品经理.md` 待验收 | 同文件 待验收 |
| 全角色 | `技术债务.md` | 架构师分析时读取 |
| 全角色 | `技术规范.md` | 编码前读取 |

---

## 常见操作场景

### 开始新迭代
终端 A: 产品经理发布需求（使用需求模板）。
终端 B: 等待架构师任务或先预热开发环境。

### 修改需求
终端 A: 产品经理更新需求，终端 B 的开发者读取更新后的文件继续工作。

### 修复 Bug
终端 B: 测试工程师发现 Bug -> 终端 A: 产品经理发布修复需求 -> 终端 B: 正常流转修复。

### 并行开发
终端 B: 前后端串行或切换开发，无需等待对方。终端 A: 测试工程师合并两个角色的交付后统一测试。

### 切换角色
终端 A: 同一个会话重新指定角色指令。
终端 B: 先切换模型（`/switch xxx` 或 `--model xxx`），再输入新角色指令。

### 处理技术债务
任何角色发现债务 -> 更新 `技术债务.md` -> 产品经理定期审查 -> 决定转化为任务 -> 正常流转。

---

## 文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `项目进度.md` | 入口 | 项目全貌，工作流状态 |
| `技术规范.md` | 规范 | 编码标准，所有角色编码前必读 |
| `技术债务.md` | 追踪 | 已知架构债务 |
| `产品经理.md` | 角色 | 需求管理、验收 |
| `架构师.md` | 角色 | 架构分析、任务分配 |
| `前端工程师.md` | 角色 | UI 开发 |
| `后端工程师.md` | 角色 | 数据层开发 |
| `测试工程师.md` | 角色 | 测试执行、报告 |
| `迭代回顾.md` | 回顾 | 迭代总结 |
| `claude.md` | 语言 | 简体中文规则 |
| `架构决策/AD-xxx.md` | ADR | 架构决策记录 |
| `归档/迭代历史.md` | 归档 | 历史迭代详细记录 |

---

## 最后更新

2026-05-29 — 重写为双终端操作模型，工具无关抽象化（当前: antigravity cli + claude code）
